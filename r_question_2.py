tup = 'Hello'
print(len(tup))
print(type(tup))

# print len is printing the length of the variable the number of characters in the word hello as its is a string

tup = 'Hello',
print(len(tup))
print(type(tup))
# tup is a tuple with one element in it and that is what is being returned
# the comma is what is defining this as a tuple